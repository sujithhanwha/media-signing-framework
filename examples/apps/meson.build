if (get_option('signer') or get_option('build_all_apps'))
  subdir('signer')
endif
if (get_option('validator') or get_option('build_all_apps'))
  subdir('validator')
endif
if (get_option('ffmpeg_signer') or get_option('build_all_apps'))
  subdir('ffmpeg-signer')
endif
